# Calculator profile configuration

Service calculator settings are stored in the existing `services.lead_form` JSON field and edited through Filament's **Lead form and advanced calculator** section.

`DefaultCalculatorProfiles` provides safe indicative defaults for services that have not yet received custom calculator pricing. Setting `calculator_enabled` to `false` hides a service from the calculator.
